---
phase: 09-implement-the-bug-001-fix-and-produce-an-upstream-pr-report
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/wsServer.ts
  - apps/server/src/wsServer.test.ts
autonomous: true
requirements:
  - FIX-01
  - FIX-02
user_setup: []
must_haves:
  truths:
    - projects.writeFile rejects a symlink-assisted path that would resolve outside the workspace root before any out-of-root mutation occurs.
    - Legitimate relative writes inside the workspace still succeed and return the normalized relative path.
    - Regression coverage proves both the allowed in-workspace write path and the rejected symlink escape path.
  artifacts:
    - path: apps/server/src/wsServer.ts
      provides: Canonical workspace write-path enforcement before directory creation or file writes.
      min_lines: 30
      contains: resolveWorkspaceWritePath
    - path: apps/server/src/wsServer.test.ts
      provides: WebSocket-route regression coverage for normal writes and symlink escape rejection.
      min_lines: 30
      contains: projects.writeFile
  key_links:
    - from: apps/server/src/wsServer.ts
      to: apps/server/src/wsServer.test.ts
      via: Route-level tests exercise the projects.writeFile behavior and reject the symlink escape path.
      pattern: projects\\.writeFile
---

<objective>
Implement the BUG-001 remediation in the server write route and lock it in with regression tests.

Purpose: Eliminate the workspace escape before any filesystem mutation can occur and make the fix durable.
Output: Hardened write-path resolution in the server plus targeted regression tests for the vulnerable path.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/BUG_CATALOG.md
@.planning/phases/09-implement-the-bug-001-fix-and-produce-an-upstream-pr-report/09-RESEARCH.md
@apps/server/src/wsServer.ts
@apps/server/src/wsServer.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Canonicalize workspace write resolution before mutation</name>
  <files>apps/server/src/wsServer.ts</files>
  <action>Refactor the `projects.writeFile` path resolver so it keeps the current lexical guard, then validates the canonical workspace boundary before any `makeDirectory()` or `writeFileString()` call. Use the canonical workspace root plus the deepest existing ancestor of the requested target so new-file writes are handled safely. Return a clear route error when the canonicalized path escapes the workspace or relies on a symlinked ancestor outside the tree. Keep the route shape and success payload stable.</action>
  <verify>Read the updated resolver flow in `apps/server/src/wsServer.ts` and confirm the write path is derived only after canonical boundary checks.</verify>
  <done>The route rejects the BUG-001 escape path before mutating outside the workspace and still accepts ordinary in-root writes.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for the symlink escape and the preserved happy path</name>
  <files>apps/server/src/wsServer.test.ts</files>
  <action>Add a deterministic regression test that creates a workspace temp directory, an out-of-tree temp directory, and a symlink inside the workspace pointing outward. Drive the vulnerable path through the WebSocket `projects.writeFile` method, assert the request fails, and prove the outside file was not created. Keep the existing in-workspace write test green, and add or refine a nested in-root write assertion if the resolver changes need extra coverage.</action>
  <verify>`bun run --cwd apps/server test src/wsServer.test.ts` passes with the new regression coverage.</verify>
  <done>Server tests reproduce the vulnerable setup and prove the fixed route blocks the escape without regressing allowed writes.</done>
</task>

<task type="auto">
  <name>Task 3: Run repo verification for the hardened route</name>
  <files>apps/server/src/wsServer.ts, apps/server/src/wsServer.test.ts</files>
  <action>Run the targeted server test first, then the repo-required verification commands so the change is ready to land without follow-up cleanup. If verification reveals unrelated pre-existing warnings, note them in the summary rather than broadening scope.</action>
  <verify>`bun run fmt`, `bun run lint`, `bun run typecheck`, and `bun run test` all succeed after the BUG-001 fix lands.</verify>
  <done>The server fix is validated by both focused regression coverage and the standard repo verification suite.</done>
</task>
</tasks>

<verification>
- [ ] `bun run --cwd apps/server test src/wsServer.test.ts` succeeds with the new regression coverage
- [ ] `bun run fmt` succeeds
- [ ] `bun run lint` succeeds
- [ ] `bun run typecheck` succeeds
- [ ] `bun run test` succeeds
</verification>

<success_criteria>

- All tasks completed
- The symlink escape is rejected before any out-of-root mutation occurs
- Normal in-workspace writes still succeed
- Regression tests make the BUG-001 failure mode durable
  </success_criteria>

<output>
After completion, create `.planning/phases/09-implement-the-bug-001-fix-and-produce-an-upstream-pr-report/09-01-SUMMARY.md`
</output>
